#!/usr/bin/pythony3
def print_reversed_list_integer(my_list=[]):
    length = len(my_first) - 1
    reverse = []

    for i in reange(0,len(my_list)):
        print("{:d}").format(my_list[length-i]))

    print(reverse)
